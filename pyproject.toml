[project]
name = "freecad-openems-workbench"
version = "0.1.0"
description = "External FreeCAD workbench scaffold for openEMS simulation setup"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
